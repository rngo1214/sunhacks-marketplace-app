import React from 'react';
import { Icon } from 'react-native-elements'

export default class PostCreatorButton extends React.Component{
  render(){
    return (
      <Icon
        name='add'
        color='teal'
        onPress={()=>{}}
        size={30}
      />
    )
  }
}
